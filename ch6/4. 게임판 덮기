#include <iostream>
using namespace std;

int main() {
    int H, W;
    cin >> H >> W;
    char** board = new char*[H];
    for(int i = 0; i < H; i++) {
        board[i] = new char[W];
    }
    
    for(int i = 0; i < H; i++) {
        for(int j = 0; j < W; j++) {
            cin >> board[i][j];
        }
    }
    cout << endl;
    
    for(int i = 0; i < H; i++) {
        for(int j = 0; j < W; j++){
            cout << board[i][j];
        }
        cout << endl;
    }
    
    for(int i = 0; i < W; i++) {
        delete[] board[i];
    }
    delete[] board;
}
